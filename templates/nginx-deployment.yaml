apiVersion: apps/v1
kind: Deployment
metadata:
  name: mynginx
spec:
  replicas: 7
  selector:
    matchLabels:
      run: mynginx
  template:
    metadata:
      labels:
        run: mynginx
    spec:
      containers:
      - image: vsv21gml/nginx:{{ .Values.image.tag }}
        name: mynginx
        ports:
        - containerPort: 80